# 後端 API 位置。留空 = 同源（由 Caddy 反向代理 /status /feed 到後端）。
# 如果前端同後端唔同網域，先填後端完整網址，例如 https://api.kelvinp.work
VITE_API_BASE=

# 直播串流網址（等你 RTSP cam + go2rtc 好咗先填）。
# 留空會顯示「直播準備中」placeholder。
# 例：go2rtc 的 HLS  
VITE_STREAM_URL=
